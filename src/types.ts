export interface CharPixels {
  [key: string]: Uint8ClampedArray
}

export interface CharSums {
  [key: string]: number
}

export interface RGBA {
  r: number,
  g: number,
  b: number,
  a?: number
}
